1. Раазнеси логику из ex_UiXmlProvider по соответствующим именам xml.
2. Нужно добавить логику пост обработки полученых результатов. 
   Причем правила для рендера/отображения таких пост-слоев будут прописаны
   в отдельном xml.
3. Нужно на странице результата добавить небольшую панель управления (подобно конфигу),
   чтобы не переключаться каждый раз между листами. На основном Dev листе будет настраиваться
   большинство параметров, а на странице результата будет поле для ввода нового ключа, по которому ищем,
   и будет кнопка для старта поиска, в углу листа.
4. Если нет vba папки то не показывать кнопку UpdateCode, проверять это в Startup.

5. Нужно улучшить поиск по части ключа. Если пользователь ввел только фамилию
   и таких фамили много, нужно на странице результата вывести весь список фамилий из таблицы состояния 
   (по сути просто в таблице состояния будет не 1 строка а список строк-кандидатов),
   и вывести уведомление желтое перед таблицей о том чтоб пользователь выбрал из списка нужного
   кандидата и заново запустил поиск.


   А вообще же может быть так что нужного ключа (при поиске по части, например по фамилии)
   в таблице состояний вообще такого не будет, зато нужная нам фамилия будет в таблице событий.
   В общем я думаю не стоит ограничиваться только таблицей состояния, нужно просто в каждой таблице в которой 
   нашлось совпадение по фамилии (части ключа) вывести просто одной строкой эту фамилию (т.е. для таблице состояния
   по замыслу и так будет 1 строка на фамилию, а вот для таблиц событий обычнно 1 ФИО может ассоциироваться с разными событиями,
   но именно в нашем случае когда мы ищем только по части ключа, мы должны в таблиах состояния вывести только ФИО-кандидата в 1 строки, 
   даже если такому кандидату соответствует несколько строк, суть в том чтобы не захламлять таблицу событий дубликатами, ведь цель выбрать нужный ФИО ключ и запустить нормалььный поиск)